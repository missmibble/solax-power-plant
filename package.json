{
  "name": "powerplant",
  "version": "1.0.0",
  "description": "SolaX Cloud monitoring app — poller, dashboard, and alerts for a home solar + battery system",
  "license": "MIT",
  "bin": {
    "powerplant": "bin/powerplant.js"
  },
  "scripts": {
    "build": "echo \"No build step required for JavaScript CDK\" && exit 0",
    "cdk": "cdk",
    "test": "jest",
    "install-all": "for d in lambda/*/ ; do (cd \"$d\" && echo \"Installing in $d\" && npm install); done",
    "deploy:dev": "bash scripts/deploy.sh dev",
    "deploy:local": "bash scripts/deploy.sh --config dev-powerplant.local.json",
    "diff": "cdk diff",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "aws-cdk": "^2.1106.1",
    "jest": "^30.2.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.239.0",
    "constructs": "^10.5.1"
  }
}
